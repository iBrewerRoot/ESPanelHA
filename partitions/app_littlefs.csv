# Custom partition table for a 16MB flash: large app + OTA + a filesystem for
# the web portal assets and the persisted entity selection.
# The filesystem partition is named "spiffs" because the Arduino LittleFS
# wrapper looks for that label by default (LittleFS.begin -> partition "spiffs").
# Name,   Type, SubType, Offset,   Size,   Flags
nvs,      data, nvs,     0x9000,   0x5000,
otadata,  data, ota,     0xe000,   0x2000,
app0,     app,  ota_0,   0x10000,  0x300000,
app1,     app,  ota_1,   0x310000, 0x300000,
spiffs,   data, spiffs,  0x610000, 0x9E0000,
coredump, data, coredump,0xFF0000, 0x10000,
